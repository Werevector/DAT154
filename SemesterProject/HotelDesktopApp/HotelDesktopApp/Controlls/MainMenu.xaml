<UserControl x:Class="HotelDesktopApp.Controlls.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelDesktopApp.Controlls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid x:Name="roomDataGrid" HorizontalAlignment="Left" VerticalAlignment="Top" ColumnWidth="*" Height="300" Width="213">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Room" Binding="{Binding room_ID}"/>
                <DataGridTextColumn Header="Beds" Binding="{Binding bedsNr}"/>
                <DataGridTextColumn Header="Pricing" Binding="{Binding pricing}"/>
            </DataGrid.Columns>
            </DataGrid>
        <Button x:Name="editBtn" Content="Edit" HorizontalAlignment="Left" Margin="218,264,0,0" VerticalAlignment="Top" Width="75" Click="editBtn_Click"/>

    </Grid>
</UserControl>
